name: Test Event Filters

on:
  workflow_dispatch:
  push:
    branches:
    - main
    - '!feature/*' 
  pull_request:
    types:
      - opened
      - closed
    paths-ignore:
      - README.md
    branches:
      - main
jobs:
  run-hello-world:
    runs-on: ubuntu-latest

    steps:
      # - name: Checkout code
      #   uses: actions/checkout@v2

      - name: Echo Hello World
        run: echo "Hello, World!"

      # - name: demo runner context
      #   env: 
      #     RUNNER_CONTEXT: ${{ toJson(runner) }}
      #   run: echo "$RUNNER_CONTEXT"
